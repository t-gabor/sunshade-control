import React from 'react';
import FlatButton from 'material-ui/FlatButton';

const AppBarButton = (props) => (
  <FlatButton {...props} labelStyle={{ color: "white" }} style={{ margin: "7px 0px 0px" }} />
);

export default AppBarButton;